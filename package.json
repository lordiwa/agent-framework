{
  "name": "agentic-framework",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-agent software-engineering harness built on the Claude Agent SDK.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build:plugin": "node scripts/build-plugin.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "esbuild": "^0.25.0",
    "gray-matter": "^4.0.3",
    "vitest": "^2.1.9",
    "zod": "^3.25.76"
  },
  "overrides": {
    "esbuild": "^0.25.0",
    "vite": "^6.4.2"
  }
}
